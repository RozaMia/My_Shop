венв
джанго
django-admin startproject shop .
python manage.py startapp appShop
python manage.py startapp appSettings
python manage.py migrate
python manage.py createsuperuser
тут новая тема: 
Quickstart pip install django-unfold 
В сеттингс 
INSTALLED_APPS = [
    'unfold',
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'appShop',
    'appSettings',
]

LANGUAGE_CODE = 'ru-ru'
TIME_ZONE = 'Europe/Moscow'

В аппсетингс админ User & group models
from django.contrib import admin
from django.contrib.auth.admin import UserAdmin as BaseUserAdmin
from django.contrib.auth.admin import GroupAdmin as BaseGroupAdmin
from django.contrib.auth.models import User, Group

from unfold.forms import AdminPasswordChangeForm, UserChangeForm, UserCreationForm
from unfold.admin import ModelAdmin


admin.site.unregister(User)
admin.site.unregister(Group)


@admin.register(User)
class UserAdmin(BaseUserAdmin, ModelAdmin):
    # Forms loaded from `unfold.forms`
    form = UserChangeForm
    add_form = UserCreationForm
    change_password_form = AdminPasswordChangeForm


@admin.register(Group)
class GroupAdmin(BaseGroupAdmin, ModelAdmin):
    pass

Settings options

UNFOLD = {
    "SITE_HEADER": "Магазин", (это для )
    "SITE_SUBHEADER": "Администрирование",
    "SITE_DROPDOWN": [
        {
            "icon": "Shopping_Bag_Speed",
            "title": "Ozon",
            "link": "https://www.ozon.ru",
        },
    ],
    "SIDEBAR": {
        "show_search": True,
        "command_search": True,
        "show_all_applications": True,
        "navigation": [
            {
                "title": "Пользователи и группы",
                "items" : [
                    {
                        "title": "Все пользователи",
                        "icon": "Account_Child",
                        "link": reverse_lazy("admin:auth_user_changelist")
                    }
                ]
            }
        ]
    }
}

аппШоп
модель он чисто настраивает приложение для администрирования тоесть потом в админке мы их регистрируем
библиотека 
pip install django-money

